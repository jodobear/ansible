[Unit]
Description=Simple service with API written in Go
After=network.target

[Service]
Type=simple
ExecStart={{home_folder}}/deployment/go-artifact
PIDFile=/var/run/simple

[Install]
WantedBy=default.target
